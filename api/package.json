{
  "name": "@osyle/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "tsc -p ../packages/shared && tsc -p ../packages/sdk",
    "build": "tsc",
    "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "worker": "node dist/worker.js",
    "test": "npm run build && node tests/api.test.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^10.0.1",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
